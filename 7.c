#include <stdio.h>

int main (){
    int lado,lado2;
    printf("Escreva o tamanho do lado do quadrado ");
    scanf("%d",lado);
    lado2=lado*lado;
    printf("O dobro da area do quadrado é de: %d",lado2*2);
    return 0;
}